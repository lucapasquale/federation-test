# Indica que um Product pode ser obtido desse serviço se você tiver o campo "id"
type Product @key(fields: "id") {
  id: Int!
  name: String!
  price: Int!
}

extend type Query {
  product(id: Int!): Product
}
