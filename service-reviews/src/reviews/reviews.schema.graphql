extend type Product @key(fields: "id") {
  id: Int! @external

  # Adiciona um campo de reviews no type externo
  reviews: [Review!]!
}

type Review {
  id: Int!
  productId: Int!
  reviewText: String!
  product: Product!
}

extend type Query {
  reviews: [Review!]!
}
